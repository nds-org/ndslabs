# Run workbench in minikube
sudo: required

env:
- CHANGE_MINIKUBE_NONE_USER=true

before_script:
  - git clone https://github.com/nds-org/ndslabs-deploy-tools.git
  - cd ndslabs-deploy-tools
  - git checkout travis_test
  - cd ..
  - curl -Lo kubectl https://storage.googleapis.com/kubernetes-release/release/v1.7.0/bin/linux/amd64/kubectl && chmod +x kubectl && sudo mv kubectl /usr/local/bin/
  - curl -Lo minikube https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64 && chmod +x minikube && sudo mv minikube /usr/local/bin/
  - sudo minikube start --vm-driver=none --kubernetes-version=v1.8.0
  - minikube update-context
  - JSONPATH='{range .items[*]}{@.metadata.name}:{range @.status.conditions[*]}{@.type}={@.status};{end}{end}'; until kubectl get nodes -o jsonpath="$JSONPATH" 2>&1 | grep -q "Ready=True"; do sleep 1; done
  - BASE_URL=$(minikube ip)



script:
  - cd apiserver; docker build -t apiserver .
  - cd ../ndslabs-deploy-tools
  - cat config.template.yaml| sed -e "s#{{[ ]*DOMAIN[ ]*}}#travis#g" > config.yaml
  - cat ~/.kube/config
  - make workbench APISERVER=apiserver APISERVER_PULL_POLICY=Never
  - scripts/wait_for_apiserver.sh $BASE_URL travis.ndslabs.org 20
  - kubectl get pod
  - make label-workers
  - make demo-login
